/**
 * production environment variables
 *
 * The variables exported from this file are injected in the app at build time
 * when the OS environment variable DIET_SAVER_ENVIRONMENT is set to "production"
 */

module.exports = {
  API_URL: "http://104.248.40.34/graphql",
};
